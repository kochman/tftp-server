# TFTP server

A basic TFTP server, implemented in C++ according to [RFC 1350](https://tools.ietf.org/html/rfc1350).

## Team

- Rex Hu - hur@rpi.edu
- Sidney Kochman - kochms@rpi.edu

## Development

```
make
```
then
```
./main.out --log
```

The `log` argument will cause it to print some useful messages about what it's doing. It can be omitted.
